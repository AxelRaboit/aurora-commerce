backend:
  mountPoints:
    intro: 'Manage connections to external resources (databases, APIs, SFTP).'
    searchPlaceholder: Search a mount point…
    add: Add mount point
    edit: Edit
    empty: No mount points configured.
    name: Name
    type: Type
    host: Host
    port: Port
    username: Username
    password: Password
    passwordPlaceholder: Leave empty to keep the current password
    database: Database
    sshPublicKey: SSH public key
    sshPublicKeyPlaceholder: ssh-ed25519 AAAA…
    sshTunnel: SSH tunnel
    sshTunnelHint: Connect to the database through an SSH tunnel.
    sshHost: SSH host
    sshHostPlaceholder: e.g. 72.60.130.238
    sshPort: SSH port
    sshUser: SSH user
    sshUserPlaceholder: e.g. ubuntu
    sshPrivateKey: SSH private key
    sshPrivateKeyPlaceholder: "-----BEGIN OPENSSH PRIVATE KEY-----\n…"
    sshPrivateKeyHint: Leave empty to keep the current key.
    namePlaceholder: e.g. Production DB
    hostPlaceholder: e.g. 192.168.1.10 or db.example.com
    portPlaceholder: e.g. 5432
    usernamePlaceholder: e.g. aurora_user
    databasePlaceholder: e.g. aurora_prod
    config: Advanced config (JSON)
    test: Test connection
    testing: Testing…
    testSuccess: Connection successful.
    testFailure: Connection failed.
    testTitle: 'Connection test — {name}'
    retry: Retry
    lastTested: Last tested
    never: Never tested
    deleteConfirm: Delete this mount point?
    types:
      database: Database
      api: API
      sftp: SFTP
    errors:
      name_required: Name is required.
      name_too_long: Name must not exceed 100 characters.
      host_required: Host is required.
      host_too_long: Host must not exceed 255 characters.
