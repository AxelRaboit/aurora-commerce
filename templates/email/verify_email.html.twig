{% extends 'email/layout/base.html.twig' %}

{% block content %}
<h1>{{ 'mail.verify_email.heading'|trans }}</h1>
<p>{{ 'mail.verify_email.greeting'|trans({'{name}': userName}) }}</p>
<p>{{ 'mail.verify_email.intro'|trans({'{siteName}': siteName ?? 'Velox'}) }}</p>

{{ include('email/partials/action_button.html.twig', {
    url: verifyUrl,
    label: 'mail.verify_email.cta'|trans,
}) }}

{% if expiresAt %}
<p class="warning">{{ 'mail.verify_email.expires'|trans({'{date}': expiresAt|date('d/m/Y H:i')}) }}</p>
{% endif %}

<div class="subcopy">
    <p class="sub">{{ 'mail.verify_email.fallback_intro'|trans }}</p>
    <p class="fallback"><a href="{{ verifyUrl }}">{{ verifyUrl }}</a></p>
</div>

<p class="warning">{{ 'mail.verify_email.footer_note'|trans }}</p>
{% endblock %}
