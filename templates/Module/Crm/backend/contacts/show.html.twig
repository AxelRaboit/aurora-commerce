{% extends '@Core/backend/layout.html.twig' %}

{% block title %}{{ contact.fullName }} - {{ 'backend.nav.contacts'|trans }} - {{ parent() }}{% endblock %}

{% block page_header_slot %}
    {{ include('@Shared/components/page_header.html.twig', {
        title: contact.fullName,
        crumbs: [
            {label: 'backend.modules.crm'|trans, href: path('backend_crm_contacts')},
            {label: 'backend.nav.contacts'|trans, href: backPath},
            {label: contact.fullName},
        ],
    }) }}
{% endblock %}

{% block body %}
<div {{ vue_component('crm/backend/contacts/ContactDetailApp', {
    contact: contact,
    activity: activity,
    updatePath: updatePath,
    deletePath: deletePath,
    backPath: backPath,
}) }} class="flex-1 min-w-0"></div>
{% endblock %}
