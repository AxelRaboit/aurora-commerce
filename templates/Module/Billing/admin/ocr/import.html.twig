{% extends '@Core/admin/layout.html.twig' %}

{% block title %}{{ 'admin.nav.ocr_import'|trans }} - {{ parent() }}{% endblock %}

{% block page_header_slot %}
    {{ include('@Shared/components/page_header.html.twig', {
        title: 'admin.nav.ocr_import'|trans,
        crumbs: [
            {label: 'admin.modules.billing'|trans, href: path('billing_invoices')},
            {label: 'admin.nav.invoices'|trans, href: path('billing_invoices')},
            {label: 'admin.nav.ocr_import'|trans},
        ],
    }) }}
{% endblock %}

{% block body %}
<div {{ vue_component('billing/admin/ocr/OcrImportApp', {
    recentJobs: recentJobs,
    uploadPath: uploadPath,
    jobsPath: jobsPath,
    invoicesPath: invoicesPath,
    invoiceShowPath: invoiceShowPath,
    statusUrlTemplate: statusUrlTemplate,
    retryPath: retryPath,
    deletePath: deletePath,
    validatePathTemplate: validatePathTemplate,
}) }} class="flex-1 min-w-0"></div>
{% endblock %}
