{% extends 'Front/themes/default/layout.html.twig' %}

{% block title %}{{ 'frontend.checkout.title'|trans }} — {{ context.siteName }}{% endblock %}

{% block body %}
<div {{ vue_component('ecommerce/front/checkout/CheckoutApp', {
    cart: cart,
    initialForm: initialForm,
    requiresShipping: requiresShipping,
    countries: countries,
    stripePublicKey: stripePublicKey,
    submitPath: submitPath,
    locale: locale,
}) }}></div>
{% endblock %}
