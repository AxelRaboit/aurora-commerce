{% extends 'Front/themes/default/layout.html.twig' %}

{% block title %}{{ 'front.register.title'|trans }} — {{ context.siteName }}{% endblock %}

{% block body %}
<div {{ vue_component('core/front/auth/RegisterApp', {
    registerPath: path('frontend_register', {locale: locale}),
    loginPath: path('frontend_login', {locale: locale}),
    registrationEnabled: registrationEnabled,
    initialErrors: errors,
    values: {name: values.name ?? '', email: values.email ?? ''},
}) }}></div>
{% endblock %}
