{% extends 'Front/themes/default/layout.html.twig' %}

{% block title %}{{ 'frontend.verify_email.title'|trans }} — {{ context.siteName }}{% endblock %}

{% block body %}
<div {{ vue_component('core/front/auth/VerifyEmailApp', {
    success: success,
    loginPath: path('frontend_login', {locale: locale}),
    registerPath: path('frontend_register', {locale: locale}),
}) }}></div>
{% endblock %}
