{% extends 'Front/themes/default/layout.html.twig' %}

{% block title %}{{ 'front.cart.title'|trans }} — {{ context.siteName }}{% endblock %}

{% block body %}
<div {{ vue_component('ecommerce/front/CartApp', {
    initialCart: cart,
    locale: locale,
    updatePath: path('front_cart_update', {locale: locale}),
    removePath: path('front_cart_remove', {locale: locale}),
    shopPath: path('front_shop_index', {locale: locale}),
    checkoutPath: path('front_checkout', {locale: locale}),
}) }}></div>
{% endblock %}
