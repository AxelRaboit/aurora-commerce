{% extends 'base_guest.html.twig' %}

{% block title %}{{ 'auth.register.title'|trans }} - {{ parent() }}{% endblock %}

{% block hero %}
    {% if registrationEnabled %}
        {{ include('components/auth_hero.html.twig') }}
    {% endif %}
{% endblock %}

{% block content %}
    <div {{ vue_component('AdminRegisterApp', {
        registerPath: path('admin_register'),
        loginPath: path('admin_login'),
        registrationEnabled: registrationEnabled,
        initialErrors: errors,
        values: {name: values.name ?? '', email: values.email ?? ''},
    }) }}></div>
{% endblock %}
