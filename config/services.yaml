# yaml-language-server: $schema=../vendor/symfony/dependency-injection/Loader/schema/services.schema.json

# This file is the entry point to configure your own services.
# Files in the packages/ subdirectory configure your dependencies.
# See also https://symfony.com/doc/current/service_container/import.html

# Put parameters here that don't need to change on each machine where the app is deployed
# https://symfony.com/doc/current/best_practices.html#use-parameters-for-application-configuration
parameters:
    app.admin_email: '%env(ADMIN_EMAIL)%'
    app.mailer_from: '%env(MAILER_FROM)%'

services:
    # default configuration for services in *this* file
    _defaults:
        autowire: true      # Automatically injects dependencies in your services.
        autoconfigure: true # Automatically registers your services as commands, event subscribers, etc.

    # makes classes in src/ available to be used as services
    # this creates a service per class whose id is the fully-qualified class name
    App\:
        resource: '../src/'

    App\Core\Auth\Manager\AccessRequestManager:
        arguments:
            $adminEmail: '%app.admin_email%'
            $mailerFrom: '%app.mailer_from%'

    App\Core\Auth\Manager\InvitationManager:
        arguments:
            $mailerFrom: '%app.mailer_from%'

    App\Module\Editorial\Form\Manager\FormManager:
        arguments:
            $mailerFrom: '%app.mailer_from%'


    App\Core\Auth\Manager\PasswordResetManager:
        arguments:
            $mailerFrom: '%app.mailer_from%'

    App\Core\Auth\Manager\EmailVerificationManager:
        arguments:
            $mailerFrom: '%app.mailer_from%'

    App\Core\Twig\VersionExtension:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Core\Theme\Service\ThemeResolver:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Core\Theme\Manager\ThemeManager:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Core\Setting\Command\DumpJsTranslationsCommand:
        arguments:
            $projectDir: '%kernel.project_dir%'

    App\Core\Module\ModuleRegistry:
        arguments:
            $modules: !tagged_iterator aurora.module

    App\Core\Module\PermissionRegistry:
        arguments:
            $modules: !tagged_iterator aurora.module

    App\Core\CoreModule:
        tags: [aurora.module]

    App\Module\Editorial\EditorialModule:
        tags: [aurora.module]

    App\Module\Crm\CrmModule:
        tags: [aurora.module]

    App\Module\Erp\ErpModule:
        tags: [aurora.module]

    App\Module\Ecommerce\EcommerceModule:
        tags: [aurora.module]
